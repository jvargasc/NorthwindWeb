@model IEnumerable<Northwind.Models.Shippers>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShipperId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ShipperId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CompanyName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Phone)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-shipperId="@item.ShipperId">Edit</a> |
                    <a asp-action="Details" asp-route-shipperId="@item.ShipperId">Details</a> |
                    <a asp-action="Delete" asp-route-shipperId="@item.ShipperId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if ((int)ViewData["page"] != 0)
{
    <partial name="../Shared/Components/_PaginationControl" />
}